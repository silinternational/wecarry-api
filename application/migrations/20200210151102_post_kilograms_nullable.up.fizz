drop_column("posts", "kilograms")
add_column("posts", "kilograms", "numeric(13,4)", {"null": true})
<%# fizz doesn't handle {"default": null} so the only way to drop the {"default": 0} is to recreate the column. %>
